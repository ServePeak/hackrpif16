<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    tools:context=".LoginScreen">

    <TextView android:id="@+id/login_screen_message"
        android:text="@string/login_screen_message"
        android:textSize="24dp"
        android:layout_width="match_parent"
        android:layout_height="wrap_content" />

    <Space android:id="@+id/login_screen_space"
        android:layout_below="@+id/login_screen_message"
        android:layout_width="match_parent"
        android:layout_height="50dp" />

    <EditText android:id="@+id/username_input"
        android:layout_below="@+id/login_screen_space"
        android:layout_centerHorizontal="true"
        android:layout_width="200dp"
        android:layout_height="wrap_content"
        android:hint="Username"/>

    <EditText android:id="@+id/password_input"
        android:layout_below="@+id/username_input"
        android:layout_centerHorizontal="true"
        android:layout_width="200dp"
        android:layout_height="wrap_content"
        android:hint="Password"
        android:inputType="textPassword"/>

    <LinearLayout
        android:layout_width="200dp"
        android:layout_height="40dp"
        android:orientation="horizontal"
        android:layout_centerHorizontal="true"
        android:layout_below="@id/password_input">

        <Button android:id="@+id/login_button"
            android:layout_below="@+id/password_input"
            android:text="Login"
            android:layout_width="100dp"
            android:layout_height="40dp"
            android:onClick="loginButton"/>

        <Button android:id="@+id/register_button"
            android:layout_below="@+id/password_input"
            android:layout_toRightOf="@+id/login_button"
            android:text="Register"
            android:layout_width="100dp"
            android:layout_height="40dp"
            android:onClick="registerButton"/>
    </LinearLayout>

</RelativeLayout>
